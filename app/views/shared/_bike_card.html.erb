<div class="card">
  <% @bikes.each do | bike | %>
    <div class="container">
    <div class="row">
      <img src=<%= bike.image %> alt="bike image" style="width:100%">
      <h4><b><%= bike.name %></b></h4>
      <p><%= bike.category %></p>
      <% end %>
    </div>
  </div>
</div>
